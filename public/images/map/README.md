Place your high-resolution Barovia map image here as `barovia-map.png`.

Recommended dimensions: ~2000x1400 pixels.
If your image differs, update MAP_WIDTH and MAP_HEIGHT in src/lib/coordinates.ts.

Use `?edit=true` in the URL to enable the coordinate picker — click on the map
and check the browser console for [x, y] pixel coordinates.
